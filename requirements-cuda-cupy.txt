# Optional CUDA development dependencies for the experimental CuPy Granger
# backend path. Keep this separate from requirements.txt so the validated CPU
# pipeline remains installable on non-GPU machines.
cupy-cuda12x>=13,<14
nvidia-cuda-runtime-cu12>=12,<13
nvidia-cuda-nvrtc-cu12>=12,<13
nvidia-cusolver-cu12>=11,<12
numba>=0.59,<1
